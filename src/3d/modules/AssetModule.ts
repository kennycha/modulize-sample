import Module from "./Module";

export default class AssetModule extends Module {
  public reduxObservedStateKeys = ['asset']

  public initialize(): void {
    
  }
  
  public dispose(): void {
    
  }
}